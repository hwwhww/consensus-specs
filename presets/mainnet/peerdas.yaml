# Mainnet preset - PeerDAS

# Misc
# ---------------------------------------------------------------
# uint64(get_generalized_index(BeaconBlockBody, 'blob_kzg_commitments'))
KZG_COMMITMENTS_MERKLE_PROOF_INDEX: 27
# uint64(2**6)
NUMBER_OF_COLUMNS: 32
# `FIELD_ELEMENTS_PER_BLOB * 2 // NUMBER_OF_COLUMNS`
FIELD_ELEMENTS_PER_CELL: 64
